import { bootstrapApplication } from '@angular/platform-browser';
import { TableroComponent } from "./app/tablero/tablero.component";

bootstrapApplication(TableroComponent)
  .catch((err) => console.error(err));
